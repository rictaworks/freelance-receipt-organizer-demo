# フロント（Next.js）環境変数の雛形。実値は .env.local（開発）/ Vercel の環境変数（本番）に置く。
# このファイルはコミットする雛形であり、実値は書かない。

# Rails API のベース URL。
# 開発: http://localhost:4000
# 本番: Railway の API サービスの公開 URL（例: https://freelance-receipt-organizer-demo-api.rictaworks.jp）
NEXT_PUBLIC_API_BASE=http://localhost:4000
